---
id: "PIXI.WRAP_MODES.pixi_constants"
title: "Enum: PIXI.WRAP_MODES"
sidebar_label: "PIXI.WRAP_MODES"
custom_edit_url: null
---

[@pixi/constants](../modules/pixi_constants).WRAP_MODES

The wrap modes that are supported by pixi.
The wrap mode affects the default wrapping mode of future operations. It can be re-assigned to either CLAMP or REPEAT, depending upon suitability. If the texture is non power of two then clamp will be used regardless as WebGL can only use REPEAT if the texture is po2.

This property only affects WebGL.



// TODO: @see

        

## Summary
| Name                         | Default | Description                                    |
| ---------------------------- | ------- | ---------------------------------------------- |
| <code>CLAMP</code>           | 33071   | The textures uvs are clamped                   |
| <code>MIRRORED_REPEAT</code> | 33648   | The texture uvs tile and repeat with mirroring |
| <code>REPEAT</code>          | 10497   | The texture uvs tile and repeat                |

    