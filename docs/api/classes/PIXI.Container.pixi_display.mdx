---
id: "PIXI.Container.pixi_display"
title: "Class: PIXI.Container"
sidebar_label: "PIXI.Container"
custom_edit_url: null
---


<div class="package-name">

[@pixi/display](../modules/pixi_display).Container

</div>


Container is a general-purpose display object that holds children. It also adds built-in support for advanced rendering features like masking and filtering.

It is the base class of all display objects that act as a container for other objects, including Graphics and Sprite.


```js
 import { BlurFilter, Container, Graphics, Sprite } from 'pixi.js';

 const container = new Container();
 const sprite = Sprite.from('https://s3-us-west-2.amazonaws.com/s.cdpn.io/693612/IaUrttj.png');

 sprite.width = 512;
 sprite.height = 512;

 // Adds a sprite as a child to this container. As a result, the sprite will be rendered whenever the container
 // is rendered.
 container.addChild(sprite);

 // Blurs whatever is rendered by the container
 container.filters = [new BlurFilter()];

 // Only the contents within a circle at the center should be rendered onto the screen.
 container.mask = new Graphics()
     .beginFill(0xffffff)
     .drawCircle(sprite.width / 2, sprite.height / 2, Math.min(sprite.width, sprite.height) / 2)
     .endFill();
```
      

        

<article>
### Contructor:
<h4><code>new PIXI()</code></h4>

</article>
            

## Extends
- [PIXI.DisplayObject](../classes/PIXI.DisplayObject.pixi_display)

## Members
<article>

### defaultSortableChildren 
**Type:** <code>boolean</code>

**Default Value**: <code>false</code>

**Description:** Sets the default value for the container property `sortableChildren`. If set to true, the container will sort its children by zIndex value when `updateTransform()` is called, or manually if `sortChildren()` is called.

This actually changes the order of elements in the array, so should be treated as a basic solution that is not performant compared to other solutions, such as [PixiJS Layers](https&#58;//github.com/pixijs/layers).

Also be aware of that this may not work nicely with the `addChildAt()` function, as the `zIndex` sorting may cause the child to automatically sorted to another position.

</article>


---------------------------------------

<article>

### _accessibleActive 
**Type:** <code>boolean</code>

**Default Value**: <code>false</code>


</article>


---------------------------------------

<article>

### _accessibleDiv 
**Type:** <code>boolean</code>

**Default Value**: <code>undefined</code>


</article>


---------------------------------------

<article>

### _bounds 
**Type:** <code>Bounds</code>

**Description:** The bounds object, this is used to calculate and store the bounds of the displayObject.

</article>


---------------------------------------

<article>

### _localBounds 
**Type:** <code>Bounds</code>

**Description:** Local bounds object, swapped with `_bounds` when using `getLocalBounds()`.

</article>


---------------------------------------

<article>

### accessible 
**Type:** <code>boolean</code>

**Default Value**: <code>false</code>

**Description:** Flag for if the object is accessible. If true AccessibilityManager will overlay a shadow div with attributes set

</article>


---------------------------------------

<article>

### accessibleChildren 
**Type:** <code>boolean</code>

**Default Value**: <code>true</code>

**Description:** Setting to false will prevent any children inside this container to be accessible. Defaults to true.

</article>


---------------------------------------

<article>

### accessibleHint 
**Type:** <code>string</code>

**Default Value**: <code>undefined</code>

**Description:** Sets the aria-label attribute of the shadow div

</article>


---------------------------------------

<article>

### accessiblePointerEvents 
**Type:** <code>string</code>

**Default Value**: <code>'auto'</code>

**Description:** Specify the pointer-events the accessible div will use Defaults to auto.

</article>


---------------------------------------

<article>

### accessibleTitle 
**Type:** <code>?string</code>

**Default Value**: <code>undefined</code>

**Description:** Sets the title attribute of the shadow div If accessibleTitle AND accessibleHint has not been this will default to 'displayObject [tabIndex]'

</article>


---------------------------------------

<article>

### accessibleType 
**Type:** <code>string</code>

**Default Value**: <code>'button'</code>

**Description:** Specify the type of div the accessible layer is. Screen readers treat the element differently depending on this type. Defaults to button.

</article>


---------------------------------------

<article>

### alpha 
**Type:** <code>number</code>

**Description:** The opacity of the object.

</article>


---------------------------------------

<article>

### angle 
**Type:** <code>number</code>

**Description:** The angle of the object in degrees. 'rotation' and 'angle' have the same effect on a display object; rotation is in radians, angle is in degrees.

</article>


---------------------------------------

<article>

### cacheAsBitmap 
**Type:** <code>boolean</code>

**Description:** Set this to true if you want this display object to be cached as a bitmap. This basically takes a snapshot of the display object as it is at that moment. It can provide a performance benefit for complex static displayObjects. To remove simply set this property to `false`

IMPORTANT GOTCHA - Make sure that all your textures are preloaded BEFORE setting this property to true as it will take a snapshot of what is currently there. If the textures have not loaded then they will not appear.

</article>


---------------------------------------

<article>

### cacheAsBitmapMultisample 
**Type:** <code>number</code>

**Default Value**: <code>null</code>

**Description:** The number of samples to use for cacheAsBitmap. If set to `null`, the renderer's sample count is used. If `cacheAsBitmap` is set to `true`, this will re-render with the new number of samples.

</article>


---------------------------------------

<article>

### cacheAsBitmapResolution 
**Type:** <code>number</code>

**Default Value**: <code>null</code>

**Description:** The resolution to use for cacheAsBitmap. By default this will use the renderer's resolution but can be overriden for performance. Lower values will reduce memory usage at the expense of render quality. A falsey value of `null` or `0` will default to the renderer's resolution. If `cacheAsBitmap` is set to `true`, this will re-render with the new resolution.

</article>


---------------------------------------

<article>

### children 
**Type:** <code>T[]</code>

**Description:** The array of children of this container.

</article>


---------------------------------------

<article>

### cullable 
**Type:** <code>boolean</code>

**Description:** Should this object be rendered if the bounds of this object are out of frame?
Culling has no effect on whether updateTransform is called.

</article>


---------------------------------------

<article>

### cullArea 
**Type:** <code>Rectangle</code>

**Description:** If set, this shape is used for culling instead of the bounds of this object. It can improve the culling performance of objects with many children. The culling area is defined in local space.

</article>


---------------------------------------

<article>

### destroyed 
**Type:** <code>boolean</code>

**Description:** Readonly flag for destroyed display objects.

</article>


---------------------------------------

<article>

### filterArea 
**Type:** <code>Rectangle</code>

**Description:** The area the filter is applied to. This is used as more of an optimization rather than figuring out the dimensions of the displayObject each frame you can set this rectangle.

Also works as an interaction mask.

</article>


---------------------------------------

<article>

### filters 
**Type:** <code>Filter[] &#124; null</code>

**Description:** Sets the filters for the displayObject. IMPORTANT&#58; This is a WebGL only feature and will be ignored by the canvas renderer. To remove filters simply set this property to `'null'`.

</article>


---------------------------------------

<article>

### height 
**Type:** <code>number</code>

**Description:** The height of the Container, setting this will actually modify the scale to achieve the value set.

</article>


---------------------------------------

<article>

### hitArea 
**Type:** <code>PIXI.IHitArea</code>

**Default Value**: <code>undefined</code>

**Description:** Interaction shape. Children will be hit first, then this shape will be checked. Setting this will cause this shape to be checked in hit tests rather than the displayObject's bounds.


```js
 import { Rectangle, Sprite } from 'pixi.js';

 const sprite = new Sprite(texture);
 sprite.interactive = true;
 sprite.hitArea = new Rectangle(0, 0, 100, 100);
```
      

</article>


---------------------------------------

<article>

### interactive 
**Type:** <code>unknown</code>

**Description:** Enable interaction events for the DisplayObject. Touch, pointer and mouse There is 4 types of interaction settings&#58;

* `'none'`&#58; Ignores all interaction events, even on its children.
* `'passive'`&#58; Does not emit events and ignores all hit testing on itself and non-interactive children. Interactive children will still emit events.
* `'auto'`&#58; Does not emit events and but is hit tested if parent is interactive. Same as `interactive = false` in v7
* `'static'`&#58; Emit events and is hit tested. Same as `interaction = true` in v7
* `'dynamic'`&#58; Emits events and is hit tested but will also receive mock interaction events fired from a ticker to allow for interaction when the mouse isn't moving


```js
 import { Sprite } from 'pixi.js';

 const sprite = new Sprite(texture);
 sprite.interactive = 'static';
 sprite.on('tap', (event) => {
     // Handle event
 });
```
      

</article>


---------------------------------------

<article>

### interactiveChildren 
**Type:** <code>boolean</code>

**Default Value**: <code>true</code>

**Description:** Determines if the children to the displayObject can be clicked/touched Setting this to false allows PixiJS to bypass a recursive `hitTest` function

</article>


---------------------------------------

<article>

### isMask 
**Type:** <code>boolean</code>

**Description:** Does any other displayObject use this object as a mask?

</article>


---------------------------------------

<article>

### isSprite 
**Type:** <code>boolean</code>

**Description:** Used to fast check if a sprite is.. a sprite!

</article>


---------------------------------------

<article>

### localTransform 
**Type:** <code>Matrix</code>

**Description:** Current transform of the object based on local factors&#58; position, scale, other stuff.

</article>


---------------------------------------

<article>

### mask 
**Type:** <code>Container &#124; MaskData &#124; null</code>

**Description:** Sets a mask for the displayObject. A mask is an object that limits the visibility of an object to the shape of the mask applied to it. In PixiJS a regular mask must be a [PIXI.Graphics](../classes/PIXI.Graphics.pixi_graphics) or a [PIXI.Sprite](../classes/PIXI.Sprite.pixi_sprite) object. This allows for much faster masking in canvas as it utilities shape clipping. Furthermore, a mask of an object must be in the subtree of its parent. Otherwise, `getLocalBounds` may calculate incorrect bounds, which makes the container's width and height wrong. To remove a mask, set this property to `null`.

For sprite mask both alpha and red channel are used. Black mask is the same as transparent mask.


```js
 import { Graphics, Sprite } from 'pixi.js';

 const graphics = new Graphics();
 graphics.beginFill(0xFF3300);
 graphics.drawRect(50, 250, 100, 100);
 graphics.endFill();

 const sprite = new Sprite(texture);
 sprite.mask = graphics;
```
      

</article>


---------------------------------------

<article>

### name 
**Type:** <code>string</code>

**Default Value**: <code>undefined</code>

**Description:** The instance name of the object.

</article>


---------------------------------------

<article>

### onclick 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `click` event.


```js
this.onclick = (event) => {
 //some function here that happens on click
}
```
      

</article>


---------------------------------------

<article>

### onglobalmousemove 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `globalmousemove` event.


```js
this.onglobalmousemove = (event) => {
 //some function here that happens on globalmousemove
}
```
      

</article>


---------------------------------------

<article>

### onglobalpointermove 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `globalpointermove` event.


```js
this.onglobalpointermove = (event) => {
 //some function here that happens on globalpointermove
}
```
      

</article>


---------------------------------------

<article>

### onglobaltouchmove 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `globaltouchmove` event.


```js
this.onglobaltouchmove = (event) => {
 //some function here that happens on globaltouchmove
}
```
      

</article>


---------------------------------------

<article>

### onmousedown 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `mousedown` event.


```js
this.onmousedown = (event) => {
 //some function here that happens on mousedown
}
```
      

</article>


---------------------------------------

<article>

### onmouseenter 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `mouseenter` event.


```js
this.onmouseenter = (event) => {
 //some function here that happens on mouseenter
}
```
      

</article>


---------------------------------------

<article>

### onmouseleave 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `mouseleave` event.


```js
this.onmouseleave = (event) => {
 //some function here that happens on mouseleave
}
```
      

</article>


---------------------------------------

<article>

### onmousemove 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `mousemove` event.


```js
this.onmousemove = (event) => {
 //some function here that happens on mousemove
}
```
      

</article>


---------------------------------------

<article>

### onmouseout 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `mouseout` event.


```js
this.onmouseout = (event) => {
 //some function here that happens on mouseout
}
```
      

</article>


---------------------------------------

<article>

### onmouseover 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `mouseover` event.


```js
this.onmouseover = (event) => {
 //some function here that happens on mouseover
}
```
      

</article>


---------------------------------------

<article>

### onmouseup 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `mouseup` event.


```js
this.onmouseup = (event) => {
 //some function here that happens on mouseup
}
```
      

</article>


---------------------------------------

<article>

### onmouseupoutside 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `mouseupoutside` event.


```js
this.onmouseupoutside = (event) => {
 //some function here that happens on mouseupoutside
}
```
      

</article>


---------------------------------------

<article>

### onpointercancel 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `pointercancel` event.


```js
this.onpointercancel = (event) => {
 //some function here that happens on pointercancel
}
```
      

</article>


---------------------------------------

<article>

### onpointerdown 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `pointerdown` event.


```js
this.onpointerdown = (event) => {
 //some function here that happens on pointerdown
}
```
      

</article>


---------------------------------------

<article>

### onpointerenter 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `pointerenter` event.


```js
this.onpointerenter = (event) => {
 //some function here that happens on pointerenter
}
```
      

</article>


---------------------------------------

<article>

### onpointerleave 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `pointerleave` event.


```js
this.onpointerleave = (event) => {
 //some function here that happens on pointerleave
}
```
      

</article>


---------------------------------------

<article>

### onpointermove 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `pointermove` event.


```js
this.onpointermove = (event) => {
 //some function here that happens on pointermove
}
```
      

</article>


---------------------------------------

<article>

### onpointerout 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `pointerout` event.


```js
this.onpointerout = (event) => {
 //some function here that happens on pointerout
}
```
      

</article>


---------------------------------------

<article>

### onpointerover 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `pointerover` event.


```js
this.onpointerover = (event) => {
 //some function here that happens on pointerover
}
```
      

</article>


---------------------------------------

<article>

### onpointertap 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `pointertap` event.


```js
this.onpointertap = (event) => {
 //some function here that happens on pointertap
}
```
      

</article>


---------------------------------------

<article>

### onpointerup 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `pointerup` event.


```js
this.onpointerup = (event) => {
 //some function here that happens on pointerup
}
```
      

</article>


---------------------------------------

<article>

### onpointerupoutside 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `pointerupoutside` event.


```js
this.onpointerupoutside = (event) => {
 //some function here that happens on pointerupoutside
}
```
      

</article>


---------------------------------------

<article>

### onrightclick 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `rightclick` event.


```js
this.onrightclick = (event) => {
 //some function here that happens on rightclick
}
```
      

</article>


---------------------------------------

<article>

### onrightdown 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `rightdown` event.


```js
this.onrightdown = (event) => {
 //some function here that happens on rightdown
}
```
      

</article>


---------------------------------------

<article>

### onrightup 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `rightup` event.


```js
this.onrightup = (event) => {
 //some function here that happens on rightup
}
```
      

</article>


---------------------------------------

<article>

### onrightupoutside 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `rightupoutside` event.


```js
this.onrightupoutside = (event) => {
 //some function here that happens on rightupoutside
}
```
      

</article>


---------------------------------------

<article>

### ontap 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `tap` event.


```js
this.ontap = (event) => {
 //some function here that happens on tap
}
```
      

</article>


---------------------------------------

<article>

### ontouchcancel 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `touchcancel` event.


```js
this.ontouchcancel = (event) => {
 //some function here that happens on touchcancel
}
```
      

</article>


---------------------------------------

<article>

### ontouchend 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `touchend` event.


```js
this.ontouchend = (event) => {
 //some function here that happens on touchend
}
```
      

</article>


---------------------------------------

<article>

### ontouchendoutside 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `touchendoutside` event.


```js
this.ontouchendoutside = (event) => {
 //some function here that happens on touchendoutside
}
```
      

</article>


---------------------------------------

<article>

### ontouchmove 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `touchmove` event.


```js
this.ontouchmove = (event) => {
 //some function here that happens on touchmove
}
```
      

</article>


---------------------------------------

<article>

### ontouchstart 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `touchstart` event.


```js
this.ontouchstart = (event) => {
 //some function here that happens on touchstart
}
```
      

</article>


---------------------------------------

<article>

### onwheel 
**Type:** <code>unknown</code>

**Default Value**: <code>null</code>

**Description:** Property-based event handler for the `wheel` event.


```js
this.onwheel = (event) => {
 //some function here that happens on wheel
}
```
      

</article>


---------------------------------------

<article>

### parent 
**Type:** <code>Container</code>

**Description:** The display object container that contains this display object.

</article>


---------------------------------------

<article>

### pivot 
**Type:** <code>ObservablePoint</code>

**Description:** The center of rotation, scaling, and skewing for this display object in its local space. The `position`is the projection of `pivot` in the parent's local space.

By default, the pivot is the origin (0, 0).

</article>


---------------------------------------

<article>

### position 
**Type:** <code>ObservablePoint</code>

**Description:** The coordinate of the object relative to the local coordinates of the parent.

</article>


---------------------------------------

<article>

### renderable 
**Type:** <code>boolean</code>

**Description:** Can this object be rendered, if false the object will not be drawn but the updateTransform methods will still be called.

Only affects recursive calls from parent. You can ask for bounds manually.

</article>


---------------------------------------

<article>

### rotation 
**Type:** <code>number</code>

**Description:** The rotation of the object in radians. 'rotation' and 'angle' have the same effect on a display object; rotation is in radians, angle is in degrees.

</article>


---------------------------------------

<article>

### scale 
**Type:** <code>ObservablePoint</code>

**Description:** The scale factors of this object along the local coordinate axes.
The default scale is (1, 1).

</article>


---------------------------------------

<article>

### skew 
**Type:** <code>ObservablePoint</code>

**Description:** The skew factor for the object in radians.

</article>


---------------------------------------

<article>

### sortableChildren 
**Type:** <code>boolean</code>

**Description:** If set to true, the container will sort its children by `zIndex` value when `updateTransform()` is called, or manually if `sortChildren()` is called.

This actually changes the order of elements in the array, so should be treated as a basic solution that is not performant compared to other solutions, such as [PixiJS Layers](https&#58;//github.com/pixijs/layers)

Also be aware of that this may not work nicely with the `addChildAt()` function, as the `zIndex` sorting may cause the child to automatically sorted to another position.

</article>


---------------------------------------

<article>

### sortDirty 
**Type:** <code>boolean</code>

**Description:** Should children be sorted by zIndex at the next updateTransform call.
Will get automatically set to true if a new child is added, or if a child's zIndex changes.

</article>


---------------------------------------

<article>

### transform 
**Type:** <code>Transform</code>

**Description:** World transform and local transform of this object. This will become read-only later, please do not assign anything there unless you know what are you doing.

</article>


---------------------------------------

<article>

### visible 
**Type:** <code>boolean</code>

**Description:** The visibility of the object. If false the object will not be drawn, and the updateTransform function will not be called.

Only affects recursive calls from parent. You can ask for bounds or call updateTransform manually.

</article>


---------------------------------------

<article>

### width 
**Type:** <code>number</code>

**Description:** The width of the Container, setting this will actually modify the scale to achieve the value set.

</article>


---------------------------------------

<article>

### worldAlpha 
**Type:** <code>number</code>

**Description:** The multiplied alpha of the displayObject.

</article>


---------------------------------------

<article>

### worldTransform 
**Type:** <code>Matrix</code>

**Description:** Current transform of the object based on world (parent) factors.

</article>


---------------------------------------

<article>

### worldVisible 
**Type:** <code>boolean</code>

**Description:** Indicates if the object is globally visible.

</article>


---------------------------------------

<article>

### x 
**Type:** <code>number</code>

**Description:** The position of the displayObject on the x axis relative to the local coordinates of the parent. An alias to position.x

</article>


---------------------------------------

<article>

### y 
**Type:** <code>number</code>

**Description:** The position of the displayObject on the y axis relative to the local coordinates of the parent. An alias to position.y

</article>


---------------------------------------

<article>

### zIndex 
**Type:** <code>number</code>

**Description:** The zIndex of the displayObject.
If a container has the sortableChildren property set to true, children will be automatically sorted by zIndex value; a higher value will mean it will be moved towards the end of the array, and thus rendered on top of other display objects within the same container.

</article>


---------------------------------------

<article>

### _boundsID 
**Type:** <code>number</code>

**Description:** Flags the cached bounds as dirty.

</article>


---------------------------------------

<article>

### _boundsRect 
**Type:** <code>Rectangle</code>

**Description:** Cache of this display-object's bounds-rectangle.

</article>


---------------------------------------

<article>

### _destroyed 
**Type:** <code>boolean</code>

**Description:** If the object has been destroyed via destroy(). If true, it should not be used.

</article>


---------------------------------------

<article>

### _enabledFilters 
**Type:** <code>Filter[]</code>

**Description:** Currently enabled filters.

</article>


---------------------------------------

<article>

### _lastSortedIndex 
**Type:** <code>number</code>

**Description:** Which index in the children array the display component was before the previous zIndex sort. Used by containers to help sort objects with the same zIndex, by using previous array index as the decider.

</article>


---------------------------------------

<article>

### _localBoundsRect 
**Type:** <code>Rectangle</code>

**Description:** Cache of this display-object's local-bounds rectangle.

</article>


---------------------------------------

<article>

### _mask 
**Type:** <code>Container &#124; MaskData</code>

**Description:** The original, cached mask of the object.

</article>


---------------------------------------

<article>

### _tempDisplayObjectParent 
**Type:** <code>PIXI.Container</code>


</article>


---------------------------------------

<article>

### _zIndex 
**Type:** <code>number</code>

**Description:** The zIndex of the displayObject. A higher value will mean it will be rendered on top of other displayObjects within the same container.

</article>


---------------------------------------


## Methods
<article>

### addChild 
**Type:** <code>(children&#58; PIXI.DisplayObject) -&#62; PIXI.DisplayObject</code>

**Description:** Adds one or more children to the container.
Multiple items can be added like so&#58; `myContainer.addChild(thingOne, thingTwo, thingThree)`

#### Parameters:
| Name     | Type                            | Description                                  |
| -------- | ------------------------------- | -------------------------------------------- |
| children | <code>PIXI.DisplayObject</code> | The DisplayObject(s) to add to the container |
#### Returns:
| Type                            | Description                       |
| ------------------------------- | --------------------------------- |
| <code>PIXI.DisplayObject</code> | * The first child that was added. |
</article>


---------------------------------------

<article>

### addChildAt 
**Type:** <code>(child&#58; PIXI.DisplayObject, index&#58; number) -&#62; PIXI.DisplayObject</code>

**Description:** Adds a child to the container at a specified index. If the index is out of bounds an error will be thrown

#### Parameters:
| Name  | Type                            | Description                     |
| ----- | ------------------------------- | ------------------------------- |
| child | <code>PIXI.DisplayObject</code> | The child to add                |
| index | <code>number</code>             | The index to place the child in |
#### Returns:
| Type                            | Description               |
| ------------------------------- | ------------------------- |
| <code>PIXI.DisplayObject</code> | The child that was added. |
</article>


---------------------------------------

<article>

### addEventListener 
**Type:** <code>(type&#58; string, listener&#58; EventListenerOrEventListenerObject, options&#58; AddListenerOptions) -&#62; </code>

**Description:** Unlike `on` or `addListener` which are methods from EventEmitter, `addEventListener`seeks to be compatible with the DOM's `addEventListener` with support for options.**IMPORTANT&#58;** _Only_ available if using the `@pixi/events` package.


```js
 // Tell the user whether they did a single, double, triple, or nth click.
 button.addEventListener('click', {
     handleEvent(e): {
         let prefix;

         switch (e.detail) {
             case 1: prefix = 'single'; break;
             case 2: prefix = 'double'; break;
             case 3: prefix = 'triple'; break;
             default: prefix = e.detail + 'th'; break;
         }

         console.log('That was a ' + prefix + 'click');
     }
 });

 // But skip the first click!
 button.parent.addEventListener('click', function blockClickOnce(e) {
     e.stopImmediatePropagation();
     button.parent.removeEventListener('click', blockClickOnce, true);
 }, {
     capture: true,
 });
```
      

#### Parameters:
| Name     | Type                                            | Description                               |
| -------- | ----------------------------------------------- | ----------------------------------------- |
| type     | <code>string</code>                             | The type of event to listen to.           |
| listener | <code>EventListenerOrEventListenerObject</code> | The listener callback or object.          |
| options  | <code>AddListenerOptions</code>                 | Listener options, used for capture phase. |
</article>


---------------------------------------

<article>

### calculateBounds 
**Type:** <code>() -&#62; void</code>

**Description:** Recalculates the bounds of the container.
This implementation will automatically fit the children's bounds into the calculation. Each child's bounds is limited to its mask's bounds or filterArea, if any is applied.

</article>


---------------------------------------

<article>

### containerUpdateTransform 
**Type:** <code>() -&#62; </code>

**Description:** Container default updateTransform, does update children of container. Will crash if there's no parent element.

</article>


---------------------------------------

<article>

### destroy 
**Type:** <code>(options&#58; IDestroyOptions &#124; boolean) -&#62; void</code>

**Description:** Removes all internal references and listeners as well as removes children from the display list. Do not use a Container after calling `destroy`.

#### Parameters:
| Name                | Type                                        | Description                                                                                                                 |
| ------------------- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| options             | <code>IDestroyOptions &#124; boolean</code> | Options parameter. A boolean will act as if all options have been set to that value                                         |
| options.children    | <code>boolean</code>                        | if set to true, all the children will have their destroy method called as well. 'options' will be passed on to those calls. |
| options.texture     | <code>boolean</code>                        | Only used for child Sprites if options.children is set to true Should it destroy the texture of the child sprite            |
| options.baseTexture | <code>boolean</code>                        | Only used for child Sprites if options.children is set to true Should it destroy the base texture of the child sprite       |
</article>


---------------------------------------

<article>

### disableTempParent 
**Type:** <code>(cacheParent&#58; Container) -&#62; void</code>

**Description:** Pair method for `enableTempParent`

#### Parameters:
| Name        | Type                   | Description              |
| ----------- | ---------------------- | ------------------------ |
| cacheParent | <code>Container</code> | Actual parent of element |
</article>


---------------------------------------

<article>

### dispatchEvent 
**Type:** <code>(e&#58; Event) -&#62; boolean</code>

**Description:** Dispatch the event on this [PIXI.DisplayObject](../classes/PIXI.DisplayObject.pixi_display) using the event's [PIXI.EventBoundary](../classes/PIXI.EventBoundary.pixi_events).
The target of the event is set to `this` and the `defaultPrevented` flag is cleared before dispatch.

**IMPORTANT&#58;** _Only_ available if using the `@pixi/events` package.


```js
// Reuse a click event!
button.dispatchEvent(clickEvent);
```
      

#### Parameters:
| Name | Type               | Description            |
| ---- | ------------------ | ---------------------- |
| e    | <code>Event</code> | The event to dispatch. |
#### Returns:
| Type                 | Description                                                                                                       |
| -------------------- | ----------------------------------------------------------------------------------------------------------------- |
| <code>boolean</code> | Whether the [preventDefault](../classes/PIXI.FederatedEvent.pixi_events#preventDefault)() method was not invoked. |
</article>


---------------------------------------

<article>

### displayObjectUpdateTransform 
**Type:** <code>() -&#62; </code>

**Description:** DisplayObject default updateTransform, does not update children of container. Will crash if there's no parent element.

</article>


---------------------------------------

<article>

### enableTempParent 
**Type:** <code>() -&#62; Container</code>

**Description:** Used in Renderer, cacheAsBitmap and other places where you call an `updateTransform` on root.
```
const cacheParent = elem.enableTempParent();
elem.updateTransform();
elem.disableTempParent(cacheParent);

```

#### Returns:
| Type                   | Description      |
| ---------------------- | ---------------- |
| <code>Container</code> | * Current parent |
</article>


---------------------------------------

<article>

### getBounds 
**Type:** <code>(skipUpdate&#58; boolean, rect&#58; Rectangle) -&#62; Rectangle</code>

**Description:** Calculates and returns the (world) bounds of the display object as a [Rectangle](../classes/PIXI.Rectangle.pixi_math).
This method is expensive on containers with a large subtree (like the stage). This is because the bounds of a container depend on its children's bounds, which recursively causes all bounds in the subtree to be recalculated. The upside, however, is that calling `getBounds` once on a container will indeed update the bounds of all children (the whole subtree, in fact). This side effect should be exploited by using`displayObject._bounds.getRectangle()` when traversing through all the bounds in a scene graph. Otherwise, calling `getBounds` on each object in a subtree will cause the total cost to increase quadratically as its height increases.

The transforms of all objects in a container's **subtree** and of all **ancestors** are updated. The world bounds of all display objects in a container's **subtree** will also be recalculated.

The `_bounds` object stores the last calculation of the bounds. You can use to entirely skip bounds calculation if needed.

```
const lastCalculatedBounds = displayObject._bounds.getRectangle(optionalRect);

```

Do know that usage of `getLocalBounds` can corrupt the `_bounds` of children (the whole subtree, actually). This is a known issue that has not been solved. See [getLocalBounds](../classes/PIXI.DisplayObject.pixi_display#getLocalBounds) for more details.

`getBounds` should be called with `skipUpdate` equal to `true` in a render() call. This is because the transforms are guaranteed to be update-to-date. In fact, recalculating inside a render() call may cause corruption in certain cases.

#### Parameters:
| Name       | Type                   | Description                                                                                                                                                                          |
| ---------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| skipUpdate | <code>boolean</code>   | Setting to `true` will stop the transforms of the scene graph from being updated. This means the calculation returned MAY be out of date BUT will give you a nice performance boost. |
| rect       | <code>Rectangle</code> | Optional rectangle to store the result of the bounds calculation.                                                                                                                    |
#### Returns:
| Type                   | Description                                                                       |
| ---------------------- | --------------------------------------------------------------------------------- |
| <code>Rectangle</code> | * The minimum axis-aligned rectangle in world space that fits around this object. |
</article>


---------------------------------------

<article>

### getChildAt 
**Type:** <code>(index&#58; number) -&#62; T</code>

**Description:** Returns the child at the specified index

#### Parameters:
| Name  | Type                | Description                   |
| ----- | ------------------- | ----------------------------- |
| index | <code>number</code> | The index to get the child at |
#### Returns:
| Type           | Description                             |
| -------------- | --------------------------------------- |
| <code>T</code> | * The child at the given index, if any. |
</article>


---------------------------------------

<article>

### getChildByName 
**Type:** <code>(name&#58; string, deep&#58; boolean) -&#62; PIXI.DisplayObject</code>

**Description:** Returns the display object in the container.
Recursive searches are done in a preorder traversal.

#### Parameters:
| Name | Type                 | Description                   |
| ---- | -------------------- | ----------------------------- |
| name | <code>string</code>  | Instance name.                |
| deep | <code>boolean</code> | Whether to search recursively |
#### Returns:
| Type                            | Description                        |
| ------------------------------- | ---------------------------------- |
| <code>PIXI.DisplayObject</code> | The child with the specified name. |
</article>


---------------------------------------

<article>

### getChildIndex 
**Type:** <code>(child&#58; T) -&#62; number</code>

**Description:** Returns the index position of a child DisplayObject instance

#### Parameters:
| Name  | Type           | Description                            |
| ----- | -------------- | -------------------------------------- |
| child | <code>T</code> | The DisplayObject instance to identify |
#### Returns:
| Type                | Description                                                  |
| ------------------- | ------------------------------------------------------------ |
| <code>number</code> | * The index position of the child display object to identify |
</article>


---------------------------------------

<article>

### getGlobalPosition 
**Type:** <code>(point&#58; PIXI.Point, skipUpdate&#58; boolean) -&#62; PIXI.Point</code>

**Description:** Returns the global position of the displayObject. Does not depend on object scale, rotation and pivot.

#### Parameters:
| Name       | Type                    | Description                                                                                                                                                                        |
| ---------- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| point      | <code>PIXI.Point</code> | The point to write the global value to.                                                                                                                                            |
| skipUpdate | <code>boolean</code>    | Setting to true will stop the transforms of the scene graph from being updated. This means the calculation returned MAY be out of date BUT will give you a nice performance boost. |
#### Returns:
| Type                    | Description        |
| ----------------------- | ------------------ |
| <code>PIXI.Point</code> | The updated point. |
</article>


---------------------------------------

<article>

### getLocalBounds 
**Type:** <code>(rect&#58; Rectangle, skipChildrenUpdate&#58; boolean) -&#62; Rectangle</code>

**Description:** Retrieves the local bounds of the displayObject as a rectangle object.
Calling `getLocalBounds` may invalidate the `_bounds` of the whole subtree below. If using it inside a render() call, it is advised to call `getBounds()` immediately after to recalculate the world bounds of the subtree.

#### Parameters:
| Name               | Type                   | Description                                                                                                                                    |
| ------------------ | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| rect               | <code>Rectangle</code> | Optional rectangle to store the result of the bounds calculation.                                                                              |
| skipChildrenUpdate | <code>boolean</code>   | Setting to `true` will stop re-calculation of children transforms, it was default behaviour of pixi 4.0-5.2 and caused many problems to users. |
#### Returns:
| Type                   | Description                      |
| ---------------------- | -------------------------------- |
| <code>Rectangle</code> | * The rectangular bounding area. |
</article>


---------------------------------------

<article>

### isInteractive 
**Type:** <code>() -&#62; boolean</code>

**Description:** Determines if the displayObject is interactive or not


```js
 import { Sprite } from 'pixi.js';
 const sprite = new Sprite(texture);
 sprite.interactive = 'static';
 sprite.isInteractive(); // true

 sprite.interactive = 'dynamic';
 sprite.isInteractive(); // true

 sprite.interactive = 'none';
 sprite.isInteractive(); // false

 sprite.interactive = 'passive';
 sprite.isInteractive(); // false

 sprite.interactive = 'auto';
 sprite.isInteractive(); // false
```
      

#### Returns:
| Type                 | Description                                     |
| -------------------- | ----------------------------------------------- |
| <code>boolean</code> | Whether the displayObject is interactive or not |
</article>


---------------------------------------

<article>

### removeChild 
**Type:** <code>(children&#58; PIXI.DisplayObject) -&#62; PIXI.DisplayObject</code>

**Description:** Removes one or more children from the container.

#### Parameters:
| Name     | Type                            | Description                    |
| -------- | ------------------------------- | ------------------------------ |
| children | <code>PIXI.DisplayObject</code> | The DisplayObject(s) to remove |
#### Returns:
| Type                            | Description                       |
| ------------------------------- | --------------------------------- |
| <code>PIXI.DisplayObject</code> | The first child that was removed. |
</article>


---------------------------------------

<article>

### removeChildAt 
**Type:** <code>(index&#58; number) -&#62; T</code>

**Description:** Removes a child from the specified index position.

#### Parameters:
| Name  | Type                | Description                     |
| ----- | ------------------- | ------------------------------- |
| index | <code>number</code> | The index to get the child from |
#### Returns:
| Type           | Description                 |
| -------------- | --------------------------- |
| <code>T</code> | The child that was removed. |
</article>


---------------------------------------

<article>

### removeChildren 
**Type:** <code>(beginIndex&#58; number, endIndex&#58; ) -&#62; T[]</code>

**Description:** Removes all children from this container that are within the begin and end indexes.

#### Parameters:
| Name       | Type                | Description                                                  |
| ---------- | ------------------- | ------------------------------------------------------------ |
| beginIndex | <code>number</code> | The beginning position.                                      |
| endIndex   |                     | The ending position. Default value is size of the container. |
#### Returns:
| Type             | Description                |
| ---------------- | -------------------------- |
| <code>T[]</code> | * List of removed children |
</article>


---------------------------------------

<article>

### removeEventListener 
**Type:** <code>(type&#58; string, listener&#58; EventListenerOrEventListenerObject, options&#58; RemoveListenerOptions) -&#62; </code>

**Description:** Unlike `off` or `removeListener` which are methods from EventEmitter, `removeEventListener`seeks to be compatible with the DOM's `removeEventListener` with support for options.**IMPORTANT&#58;** _Only_ available if using the `@pixi/events` package.

#### Parameters:
| Name     | Type                                            | Description                                                                             |
| -------- | ----------------------------------------------- | --------------------------------------------------------------------------------------- |
| type     | <code>string</code>                             | The type of event the listener is bound to.                                             |
| listener | <code>EventListenerOrEventListenerObject</code> | The listener callback or object.                                                        |
| options  | <code>RemoveListenerOptions</code>              | The original listener options. This is required to deregister a capture phase listener. |
</article>


---------------------------------------

<article>

### removeFromParent 
**Type:** <code>() -&#62; </code>

**Description:** Remove the DisplayObject from its parent Container. If the DisplayObject has no parent, do nothing.

</article>


---------------------------------------

<article>

### render 
**Type:** <code>(renderer&#58; Renderer) -&#62; void</code>

**Description:** Renders the object using the WebGL renderer.
The [_render](../classes/PIXI.Container.pixi_display#_render) method is be overriden for rendering the contents of the container itself. This `render` method will invoke it, and also invoke the `render` methods of all children afterward.

If `renderable` or `visible` is false or if `worldAlpha` is not positive or if `cullable` is true and the bounds of this object are out of frame, this implementation will entirely skip rendering. See [PIXI.DisplayObject](../classes/PIXI.DisplayObject.pixi_display) for choosing between `renderable` or `visible`. Generally, setting alpha to zero is not recommended for purely skipping rendering.

When your scene becomes large (especially when it is larger than can be viewed in a single screen), it is advised to employ **culling** to automatically skip rendering objects outside of the current screen. See [cullable](../classes/PIXI.DisplayObject.pixi_display#cullable) and [cullArea](../classes/PIXI.DisplayObject.pixi_display#cullArea). Other culling methods might be better suited for a large number static objects; see [&#64;pixi-essentials/cull](https&#58;//www.npmjs.com/package/&#64;pixi-essentials/cull) and [pixi-cull](https&#58;//www.npmjs.com/package/pixi-cull).

The [renderAdvanced](../classes/PIXI.Container.pixi_display#renderAdvanced) method is internally used when when masking or filtering is applied on a container. This does, however, break batching and can affect performance when masking and filtering is applied extensively throughout the scene graph.

#### Parameters:
| Name     | Type                  | Description  |
| -------- | --------------------- | ------------ |
| renderer | <code>Renderer</code> | The renderer |
</article>


---------------------------------------

<article>

### renderCanvas 
**Type:** <code>(renderer&#58; PIXI.CanvasRenderer) -&#62; void</code>

**Description:** Renders the object using the Canvas renderer

#### Parameters:
| Name     | Type                             | Description  |
| -------- | -------------------------------- | ------------ |
| renderer | <code>PIXI.CanvasRenderer</code> | The renderer |
</article>


---------------------------------------

<article>

### renderCanvas 
**Type:** <code>(_renderer&#58; PIXI.CanvasRenderer) -&#62; void</code>

**Description:** Renders the object using the Canvas renderer

#### Parameters:
| Name      | Type                             | Description  |
| --------- | -------------------------------- | ------------ |
| _renderer | <code>PIXI.CanvasRenderer</code> | The renderer |
</article>


---------------------------------------

<article>

### setChildIndex 
**Type:** <code>(child&#58; T, index&#58; number) -&#62; void</code>

**Description:** Changes the position of an existing child in the display object container

#### Parameters:
| Name  | Type                | Description                                                                    |
| ----- | ------------------- | ------------------------------------------------------------------------------ |
| child | <code>T</code>      | The child DisplayObject instance for which you want to change the index number |
| index | <code>number</code> | The resulting index number for the child display object                        |
</article>


---------------------------------------

<article>

### setParent 
**Type:** <code>(container&#58; Container) -&#62; Container</code>

**Description:** Set the parent Container of this DisplayObject.

#### Parameters:
| Name      | Type                   | Description                                 |
| --------- | ---------------------- | ------------------------------------------- |
| container | <code>Container</code> | The Container to add this DisplayObject to. |
#### Returns:
| Type                   | Description                                           |
| ---------------------- | ----------------------------------------------------- |
| <code>Container</code> | * The Container that this DisplayObject was added to. |
</article>


---------------------------------------

<article>

### setTransform 
**Type:** <code>(x&#58; number, y&#58; number, scaleX&#58; number, scaleY&#58; number, rotation&#58; number, skewX&#58; number, skewY&#58; number, pivotX&#58; number, pivotY&#58; number) -&#62; this</code>

**Description:** Convenience function to set the position, scale, skew and pivot at once.

#### Parameters:
| Name     | Type                | Description       |
| -------- | ------------------- | ----------------- |
| x        | <code>number</code> | The X position    |
| y        | <code>number</code> | The Y position    |
| scaleX   | <code>number</code> | The X scale value |
| scaleY   | <code>number</code> | The Y scale value |
| rotation | <code>number</code> | The rotation      |
| skewX    | <code>number</code> | The X skew value  |
| skewY    | <code>number</code> | The Y skew value  |
| pivotX   | <code>number</code> | The X pivot value |
| pivotY   | <code>number</code> | The Y pivot value |
#### Returns:
| Type              | Description                  |
| ----------------- | ---------------------------- |
| <code>this</code> | * The DisplayObject instance |
</article>


---------------------------------------

<article>

### sortChildren 
**Type:** <code>() -&#62; void</code>

**Description:** Sorts children by zIndex. Previous order is maintained for 2 children with the same zIndex.

</article>


---------------------------------------

<article>

### swapChildren 
**Type:** <code>(child&#58; T, child2&#58; T) -&#62; void</code>

**Description:** Swaps the position of 2 Display Objects within this container.

#### Parameters:
| Name   | Type           | Description                   |
| ------ | -------------- | ----------------------------- |
| child  | <code>T</code> | First display object to swap  |
| child2 | <code>T</code> | Second display object to swap |
</article>


---------------------------------------

<article>

### toGlobal 
**Type:** <code>(position&#58; IPointData, point&#58; P, skipUpdate&#58; boolean) -&#62; P</code>

**Description:** Calculates the global position of the display object.

#### Parameters:
| Name       | Type                    | Description                                                                               |
| ---------- | ----------------------- | ----------------------------------------------------------------------------------------- |
| position   | <code>IPointData</code> | The world origin to calculate from.                                                       |
| point      | <code>P</code>          | A Point object in which to store the value, optional (otherwise will create a new Point). |
| skipUpdate | <code>boolean</code>    | Should we skip the update transform.                                                      |
#### Returns:
| Type           | Description                                                |
| -------------- | ---------------------------------------------------------- |
| <code>P</code> | * A point object representing the position of this object. |
</article>


---------------------------------------

<article>

### toLocal 
**Type:** <code>(position&#58; IPointData, from&#58; DisplayObject, point&#58; P, skipUpdate&#58; boolean) -&#62; P</code>

**Description:** Calculates the local position of the display object relative to another point.

#### Parameters:
| Name       | Type                       | Description                                                                               |
| ---------- | -------------------------- | ----------------------------------------------------------------------------------------- |
| position   | <code>IPointData</code>    | The world origin to calculate from.                                                       |
| from       | <code>DisplayObject</code> | The DisplayObject to calculate the global position from.                                  |
| point      | <code>P</code>             | A Point object in which to store the value, optional (otherwise will create a new Point). |
| skipUpdate | <code>boolean</code>       | Should we skip the update transform                                                       |
#### Returns:
| Type           | Description                                               |
| -------------- | --------------------------------------------------------- |
| <code>P</code> | * A point object representing the position of this object |
</article>


---------------------------------------

<article>

### updateTransform 
**Type:** <code>() -&#62; void</code>

**Description:** Updates the transform on all children of this container for rendering.

</article>


---------------------------------------

<article>

### _calculateBounds 
**Type:** <code>() -&#62; void</code>

**Description:** Recalculates the content bounds of this object. This should be overriden to calculate the bounds of this specific object (not including children).

</article>


---------------------------------------

<article>

### _recursivePostUpdateTransform 
**Type:** <code>() -&#62; void</code>

**Description:** Recursively updates transform of all objects from the root to this one internal function for toLocal()

</article>


---------------------------------------

<article>

### _render 
**Type:** <code>(_renderer&#58; Renderer) -&#62; void</code>

**Description:** To be overridden by the subclasses.

#### Parameters:
| Name      | Type                  | Description  |
| --------- | --------------------- | ------------ |
| _renderer | <code>Renderer</code> | The renderer |
</article>


---------------------------------------

<article>

### _renderCanvas 
**Type:** <code>(_renderer&#58; PIXI.CanvasRenderer) -&#62; void</code>

**Description:** To be overridden by the subclass

#### Parameters:
| Name      | Type                             | Description  |
| --------- | -------------------------------- | ------------ |
| _renderer | <code>PIXI.CanvasRenderer</code> | The renderer |
</article>


---------------------------------------

<article>

### _renderWithCulling 
**Type:** <code>(renderer&#58; PIXI.Renderer) -&#62; void</code>

**Description:** Renders this object and its children with culling.

#### Parameters:
| Name     | Type                       | Description  |
| -------- | -------------------------- | ------------ |
| renderer | <code>PIXI.Renderer</code> | The renderer |
</article>


---------------------------------------

<article>

### onChildrenChange 
**Type:** <code>(_length&#58; number) -&#62; void</code>

**Description:** Overridable method that can be used by Container subclasses whenever the children array is modified.

#### Parameters:
| Name    | Type                | Description |
| ------- | ------------------- | ----------- |
| _length | <code>number</code> |             |
</article>


---------------------------------------

<article>

### renderAdvanced 
**Type:** <code>(renderer&#58; Renderer) -&#62; void</code>

**Description:** Render the object using the WebGL renderer and advanced features.

#### Parameters:
| Name     | Type                  | Description  |
| -------- | --------------------- | ------------ |
| renderer | <code>Renderer</code> | The renderer |
</article>


---------------------------------------


    