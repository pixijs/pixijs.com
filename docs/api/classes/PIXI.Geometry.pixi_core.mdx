---
id: "PIXI.Geometry.pixi_core"
title: "Class: PIXI.Geometry"
sidebar_label: "PIXI.Geometry"
custom_edit_url: null
---


<div class="package-name">

[@pixi/core](../modules/pixi_core).Geometry

</div>


The Geometry represents a model. It consists of two components&#58;

* GeometryStyle - The structure of the model such as the attributes layout
* GeometryData - the data of the model - this consists of buffers. This can include anything from positions, uvs, normals, colors etc.

Geometry can be defined without passing in a style or data if required (thats how I prefer!)


```js
 import { Geometry } from 'pixi.js';

 const geometry = new Geometry();

 geometry.addAttribute('positions', [0, 0, 100, 0, 100, 100, 0, 100], 2);
 geometry.addAttribute('uvs', [0, 0, 1, 0, 1, 1, 0, 1], 2);
 geometry.addIndex([0, 1, 2, 1, 3, 2]);
```
      

        

<article>

### Constructor:
<h4><code>new PIXI.Geometry(buffers&#58; Array&#60;Buffer&#62;, attributes&#58; &#123; [key&#58; string]&#58; Attribute &#125;) </code></h4>

#### Parameters:
| Name       | Type                                                        | Default       | Description                                                  |
| ---------- | ----------------------------------------------------------- | ------------- | ------------------------------------------------------------ |
| buffers    | <code>Array&#60;Buffer&#62;</code>                          | <code></code> | An array of buffers. optional.                               |
| attributes | <code>&#123; [key&#58; string]&#58; Attribute &#125;</code> | <code></code> | Of the geometry, optional structure of the attributes layout |

</article>

---------------------------------------




## Members
<article>

### glVertexArrayObjects 
**Type:** <code>object</code>

**Description:** A map of renderer IDs to webgl VAOs

</article>


---------------------------------------

<article>

### instanceCount 
**Type:** <code>number</code>

**Default Value**: <code>1</code>

**Description:** Number of instances in this geometry, pass it to `GeometrySystem.draw()`.

</article>


---------------------------------------

<article>

### instanced 
**Type:** <code>boolean</code>

**Description:** Whether the geometry is instanced.

</article>


---------------------------------------

<article>

### refCount 
**Type:** <code>number</code>

**Description:** Count of existing (not destroyed) meshes that reference this geometry.

</article>


---------------------------------------


## Methods
<article>

### merge 
**Type:** <code>(geometries&#58; Array&#60;Geometry&#62;) -&#62; Geometry</code>

**Description:** Merges an array of geometries into a new single one.
Geometry attribute styles must match for this operation to work.

#### Parameters:
| Name       | Type                                 | Description                  |
| ---------- | ------------------------------------ | ---------------------------- |
| geometries | <code>Array&#60;Geometry&#62;</code> | array of geometries to merge |
#### Returns:
| Type                  | Description           |
| --------------------- | --------------------- |
| <code>Geometry</code> | * Shiny new geometry! |
</article>


---------------------------------------

<article>

### addAttribute 
**Type:** <code>(id&#58; string, buffer&#58; PIXI.Buffer &#124; Array&#60;number&#62;, size&#58; number, normalized&#58; boolean, type&#58; TYPES, stride&#58; number, start&#58; number, instance&#58; boolean) -&#62; this</code>

**Description:** Adds an attribute to the geometry Note&#58; `stride` and `start` should be `undefined` if you dont know them, not 0!

#### Parameters:
| Name       | Type                                                  | Description                                                                                                            |
| ---------- | ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| id         | <code>string</code>                                   | the name of the attribute (matching up to a shader)                                                                    |
| buffer     | <code>PIXI.Buffer &#124; Array&#60;number&#62;</code> | the buffer that holds the data of the attribute . You can also provide an Array and a buffer will be created from it.  |
| size       | <code>number</code>                                   | the size of the attribute. If you have 2 floats per vertex (eg position x and y) this would be 2                       |
| normalized | <code>boolean</code>                                  | should the data be normalized.                                                                                         |
| type       | <code>TYPES</code>                                    | what type of number is the attribute. Check [PIXI.TYPES](../enums/PIXI.TYPES.pixi_constants) to see the ones available |
| stride     | <code>number</code>                                   | How far apart, in bytes, the start of each value is. (used for interleaving data)                                      |
| start      | <code>number</code>                                   | How far into the array to start reading values (used for interleaving data)                                            |
| instance   | <code>boolean</code>                                  | Instancing flag                                                                                                        |
#### Returns:
| Type              | Description                          |
| ----------------- | ------------------------------------ |
| <code>this</code> | * Returns self, useful for chaining. |
</article>


---------------------------------------

<article>

### addIndex 
**Type:** <code>(buffer&#58; PIXI.Buffer &#124; Array&#60;number&#62;) -&#62; Geometry</code>

**Description:** Adds an index buffer to the geometry The index buffer contains integers, three for each triangle in the geometry, which reference the various attribute buffers (position, colour, UV coordinates, other UV coordinates, normal, …). There is only ONE index buffer.

#### Parameters:
| Name   | Type                                                  | Description                                                                                                             |
| ------ | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| buffer | <code>PIXI.Buffer &#124; Array&#60;number&#62;</code> | The buffer that holds the data of the index buffer. You can also provide an Array and a buffer will be created from it. |
#### Returns:
| Type                  | Description                          |
| --------------------- | ------------------------------------ |
| <code>Geometry</code> | * Returns self, useful for chaining. |
</article>


---------------------------------------

<article>

### clone 
**Type:** <code>() -&#62; Geometry</code>

**Description:** Returns a clone of the geometry.

#### Returns:
| Type                  | Description                     |
| --------------------- | ------------------------------- |
| <code>Geometry</code> | * A new clone of this geometry. |
</article>


---------------------------------------

<article>

### destroy 
**Type:** <code>() -&#62; void</code>

**Description:** Destroys the geometry.

</article>


---------------------------------------

<article>

### dispose 
**Type:** <code>() -&#62; void</code>

**Description:** Disposes WebGL resources that are connected to this geometry.

</article>


---------------------------------------

<article>

### getAttribute 
**Type:** <code>(id&#58; string) -&#62; Attribute</code>

**Description:** Returns the requested attribute.

#### Parameters:
| Name | Type                | Description                        |
| ---- | ------------------- | ---------------------------------- |
| id   | <code>string</code> | The name of the attribute required |
#### Returns:
| Type                   | Description                |
| ---------------------- | -------------------------- |
| <code>Attribute</code> | * The attribute requested. |
</article>


---------------------------------------

<article>

### getBuffer 
**Type:** <code>(id&#58; string) -&#62; Buffer</code>

**Description:** Returns the requested buffer.

#### Parameters:
| Name | Type                | Description                      |
| ---- | ------------------- | -------------------------------- |
| id   | <code>string</code> | The name of the buffer required. |
#### Returns:
| Type                | Description             |
| ------------------- | ----------------------- |
| <code>Buffer</code> | * The buffer requested. |
</article>


---------------------------------------

<article>

### getIndex 
**Type:** <code>() -&#62; Buffer</code>

**Description:** Returns the index buffer

#### Returns:
| Type                | Description         |
| ------------------- | ------------------- |
| <code>Buffer</code> | * The index buffer. |
</article>


---------------------------------------

<article>

### getSize 
**Type:** <code>() -&#62; number</code>

**Description:** Get the size of the geometries, in vertices.

#### Returns:
| Type                | Description |
| ------------------- | ----------- |
| <code>number</code> |             |
</article>


---------------------------------------

<article>

### interleave 
**Type:** <code>() -&#62; Geometry</code>

**Description:** This function modifies the structure so that all current attributes become interleaved into a single buffer This can be useful if your model remains static as it offers a little performance boost

#### Returns:
| Type                  | Description                          |
| --------------------- | ------------------------------------ |
| <code>Geometry</code> | * Returns self, useful for chaining. |
</article>


---------------------------------------


    