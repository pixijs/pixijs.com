---
id: "PIXI.Texture.pixi_core"
title: "Class: PIXI.Texture"
sidebar_label: "PIXI.Texture"
custom_edit_url: null
---


<div class="package-name">

[@pixi/core](../modules/pixi_core).Texture

</div>

A texture stores the information that represents an image or part of an image.
It cannot be added to the display list directly; instead use it as the texture for a Sprite. If no frame is provided for a texture, then the whole image is used.

You can directly create a texture from an image and then reuse it multiple times like this &#58;

```
import { Sprite, Texture } from 'pixi.js';

const texture = Texture.from('assets/image.png');
const sprite1 = new Sprite(texture);
const sprite2 = new Sprite(texture);

```

If you didnt pass the texture frame to constructor, it enables `noFrame` mode&#58; it subscribes on baseTexture events, it automatically resizes at the same time as baseTexture.

Textures made from SVGs, loaded or not, cannot be used before the file finishes processing. You can check for this by checking the sprite's _textureID property.

```
import { Sprite, Texture } from 'pixi.js';

const texture = Texture.from('assets/image.svg');
const sprite1 = new Sprite(texture);
// sprite1._textureID should not be undefined if the texture has finished processing the SVG file

```

You can use a ticker or rAF to ensure your sprites load the finished textures after processing. See issue [#3085](https&#58;//github.com/pixijs/pixijs/issues/3085).



        

<article>

### Constructor:
<h4><code>new PIXI.Texture(baseTexture&#58; BaseTexture&#60;R&#62;, frame&#58; Rectangle, orig&#58; Rectangle, trim&#58; Rectangle, rotate&#58; number, anchor&#58; IPointData, borders&#58; ITextureBorders) </code></h4>

#### Parameters:
| Name        | Type                                | Default       | Description                                                                                                          |
| ----------- | ----------------------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------- |
| baseTexture | <code>BaseTexture&#60;R&#62;</code> | <code></code> | The base texture source to create the texture from                                                                   |
| frame       | <code>Rectangle</code>              | <code></code> | The rectangle frame of the texture to show                                                                           |
| orig        | <code>Rectangle</code>              | <code></code> | The area of original texture                                                                                         |
| trim        | <code>Rectangle</code>              | <code></code> | Trimmed rectangle of original texture                                                                                |
| rotate      | <code>number</code>                 | <code></code> | indicates how the texture was rotated by texture packer. See [PIXI.groupD8](../namespaces/PIXI.groupD8.pixi_math)    |
| anchor      | <code>IPointData</code>             | <code></code> | Default anchor point used for sprite placement / rotation                                                            |
| borders     | <code>ITextureBorders</code>        | <code></code> | Default borders used for 9-slice scaling. See [PIXI.NineSlicePlane](../classes/PIXI.NineSlicePlane.pixi_mesh_extras) |

</article>

---------------------------------------


## Extends
- EventEmitter

## Members
<article>

### EMPTY 
**Type:** <code>Texture&#60;Resource&#62;</code>

**Description:** An empty texture, used often to not have to create multiple empty textures. Can not be destroyed.

</article>


---------------------------------------

<article>

### WHITE 
**Type:** <code>Texture&#60;CanvasResource&#62;</code>

**Description:** A white texture of 16x16 size, used for graphics and other things Can not be destroyed.

</article>


---------------------------------------

<article>

### _frame 
**Type:** <code>Rectangle</code>

**Description:** This is the area of the BaseTexture image to actually copy to the Canvas / WebGL when rendering, irrespective of the actual frame size or placement (which can be influenced by trimmed texture atlases)

</article>


---------------------------------------

<article>

### baseTexture 
**Type:** <code>BaseTexture&#60;R&#62;</code>

**Description:** The base texture that this texture uses.

</article>


---------------------------------------

<article>

### defaultAnchor 
**Type:** <code>Point</code>

**Default Value**: <code>&#123;0,0&#125;</code>

**Description:** Anchor point that is used as default if sprite is created with this texture. Changing the `defaultAnchor` at a later point of time will not update Sprite's anchor point.

</article>


---------------------------------------

<article>

### defaultBorders 
**Type:** <code>ITextureBorders</code>

**Description:** Default width of the non-scalable border that is used if 9-slice plane is created with this texture.

</article>


---------------------------------------

<article>

### frame 
**Type:** <code>Rectangle</code>

**Description:** The frame specifies the region of the base texture that this texture uses. Please call `updateUvs()` after you change coordinates of `frame` manually.

</article>


---------------------------------------

<article>

### height 
**Type:** <code>number</code>

**Description:** The height of the Texture in pixels.

</article>


---------------------------------------

<article>

### noFrame 
**Type:** <code>boolean</code>

**Description:** Does this Texture have any frame data assigned to it?
This mode is enabled automatically if no frame was passed inside constructor.

In this mode texture is subscribed to baseTexture events, and fires `update` on any change.

Beware, after loading or resize of baseTexture event can fired two times! If you want more control, subscribe on baseTexture itself.

Any assignment of `frame` switches off `noFrame` mode.


```js
texture.on('update', () => {});
```
      

</article>


---------------------------------------

<article>

### orig 
**Type:** <code>Rectangle</code>

**Description:** This is the area of original texture, before it was put in atlas.

</article>


---------------------------------------

<article>

### resolution 
**Type:** <code>number</code>

**Description:** Returns resolution of baseTexture

</article>


---------------------------------------

<article>

### rotate 
**Type:** <code>number</code>

**Description:** Indicates whether the texture is rotated inside the atlas set to 2 to compensate for texture packer rotation set to 6 to compensate for spine packer rotation can be used to rotate or mirror sprites See [PIXI.groupD8](../namespaces/PIXI.groupD8.pixi_math) for explanation

</article>


---------------------------------------

<article>

### textureCacheIds 
**Type:** <code>Array&#60;string&#62;</code>

**Description:** The ids under which this Texture has been added to the texture cache. This is automatically set as long as Texture.addToCache is used, but may not be set if a Texture is added directly to the TextureCache array.

</article>


---------------------------------------

<article>

### trim 
**Type:** <code>Rectangle</code>

**Description:** This is the trimmed area of original texture, before it was put in atlas Please call `updateUvs()` after you change coordinates of `trim` manually.

</article>


---------------------------------------

<article>

### uvMatrix 
**Type:** <code>TextureMatrix</code>

**Description:** Default TextureMatrix instance for this texture. By default, that object is not created because its heavy.

</article>


---------------------------------------

<article>

### valid 
**Type:** <code>boolean</code>

**Description:** This will let the renderer know if the texture is valid. If it's not then it cannot be rendered.

</article>


---------------------------------------

<article>

### width 
**Type:** <code>number</code>

**Description:** The width of the Texture in pixels.

</article>


---------------------------------------

<article>

### _updateID 
**Type:** <code>number</code>

**Description:** Update ID is observed by sprites and TextureMatrix instances. Call updateUvs() to increment it.

</article>


---------------------------------------

<article>

### _uvs 
**Type:** <code>TextureUvs</code>

**Description:** The WebGL UV data cache. Can be used as quad UV.

</article>


---------------------------------------


## Methods
<article>

### addToCache 
**Type:** <code>(texture&#58; Texture, id&#58; string) -&#62; void</code>

**Description:** Adds a Texture to the global TextureCache. This cache is shared across the whole PIXI object.

#### Parameters:
| Name    | Type                 | Description                                     |
| ------- | -------------------- | ----------------------------------------------- |
| texture | <code>Texture</code> | The Texture to add to the cache.                |
| id      | <code>string</code>  | The id that the Texture will be stored against. |
</article>


---------------------------------------

<article>

### from 
**Type:** <code>(source&#58; string &#124; PIXI.BaseTexture &#124; HTMLImageElement &#124; HTMLVideoElement &#124; ImageBitmap &#124; PIXI.ICanvas, options&#58; IBaseTextureOptions&#60;RO&#62;, strict&#58; boolean) -&#62; PIXI.Texture</code>

**Description:** Helper function that creates a new Texture based on the source you provide. The source can be - frame id, image url, video url, canvas element, video element, base texture

#### Parameters:
| Name                 | Type                                                                                                                               | Description                                                                                                                    |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| source               | <code>string &#124; PIXI.BaseTexture &#124; HTMLImageElement &#124; HTMLVideoElement &#124; ImageBitmap &#124; PIXI.ICanvas</code> | Source or array of sources to create texture from                                                                              |
| options              | <code>IBaseTextureOptions&#60;RO&#62;</code>                                                                                       | See [PIXI.BaseTexture](../classes/PIXI.BaseTexture.pixi_core)'s constructor for options.                                       |
| options.pixiIdPrefix | <code>string</code>                                                                                                                | If a source has no id, this is the prefix of the generated id                                                                  |
| strict               | <code>boolean</code>                                                                                                               | Enforce strict-mode, see [PIXI.settings.STRICT_TEXTURE_CACHE](../namespaces/PIXI.settings.pixi_settings#STRICT_TEXTURE_CACHE). |
#### Returns:
| Type                      | Description               |
| ------------------------- | ------------------------- |
| <code>PIXI.Texture</code> | The newly created texture |
</article>


---------------------------------------

<article>

### fromBuffer 
**Type:** <code>(buffer&#58; Float32Array &#124; Uint8Array, width&#58; number, height&#58; number, options&#58; IBaseTextureOptions&#60;ISize&#62;) -&#62; Texture&#60;BufferResource&#62;</code>

**Description:** Create a new Texture with a BufferResource from a Float32Array. RGBA values are floats from 0 to 1.

#### Parameters:
| Name    | Type                                            | Description                                                                              |
| ------- | ----------------------------------------------- | ---------------------------------------------------------------------------------------- |
| buffer  | <code>Float32Array &#124; Uint8Array</code>     | The optional array to use, if no data is provided, a new Float32Array is created.        |
| width   | <code>number</code>                             | Width of the resource                                                                    |
| height  | <code>number</code>                             | Height of the resource                                                                   |
| options | <code>IBaseTextureOptions&#60;ISize&#62;</code> | See [PIXI.BaseTexture](../classes/PIXI.BaseTexture.pixi_core)'s constructor for options. |
#### Returns:
| Type                                         | Description                     |
| -------------------------------------------- | ------------------------------- |
| <code>Texture&#60;BufferResource&#62;</code> | * The resulting new BaseTexture |
</article>


---------------------------------------

<article>

### fromLoader 
**Type:** <code>(source&#58; HTMLImageElement &#124; HTMLVideoElement &#124; ImageBitmap &#124; PIXI.ICanvas &#124; string, imageUrl&#58; string, name&#58; string, options&#58; IBaseTextureOptions) -&#62; Promise&#60;Texture&#60;R&#62;&#62;</code>

**Description:** Create a texture from a source and add to the cache.

#### Parameters:
| Name     | Type                                                                                                       | Description                                                                                                       |
| -------- | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| source   | <code>HTMLImageElement &#124; HTMLVideoElement &#124; ImageBitmap &#124; PIXI.ICanvas &#124; string</code> | The input source.                                                                                                 |
| imageUrl | <code>string</code>                                                                                        | File name of texture, for cache and resolving resolution.                                                         |
| name     | <code>string</code>                                                                                        | Human readable name for the texture cache. If no name is specified, only `imageUrl` will be used as the cache ID. |
| options  | <code>IBaseTextureOptions</code>                                                                           |                                                                                                                   |
#### Returns:
| Type                                             | Description      |
| ------------------------------------------------ | ---------------- |
| <code>Promise&#60;Texture&#60;R&#62;&#62;</code> | * Output texture |
</article>


---------------------------------------

<article>

### fromURL 
**Type:** <code>(url&#58; string &#124; string[], options&#58; IBaseTextureOptions&#60;RO&#62;) -&#62; Promise&#60;Texture&#60;R&#62;&#62;</code>

**Description:** Useful for loading textures via URLs. Use instead of `Texture.from` because it does a better job of handling failed URLs more effectively. This also ignores`PIXI.settings.STRICT_TEXTURE_CACHE`. Works for Videos, SVGs, Images.

#### Parameters:
| Name    | Type                                         | Description                              |
| ------- | -------------------------------------------- | ---------------------------------------- |
| url     | <code>string &#124; string[]</code>          | The remote URL or array of URLs to load. |
| options | <code>IBaseTextureOptions&#60;RO&#62;</code> | Optional options to include              |
#### Returns:
| Type                                             | Description                             |
| ------------------------------------------------ | --------------------------------------- |
| <code>Promise&#60;Texture&#60;R&#62;&#62;</code> | * A Promise that resolves to a Texture. |
</article>


---------------------------------------

<article>

### removeFromCache 
**Type:** <code>(texture&#58; string &#124; Texture) -&#62; Texture &#124; null</code>

**Description:** Remove a Texture from the global TextureCache.

#### Parameters:
| Name    | Type                               | Description                                                 |
| ------- | ---------------------------------- | ----------------------------------------------------------- |
| texture | <code>string &#124; Texture</code> | id of a Texture to be removed, or a Texture instance itself |
#### Returns:
| Type                             | Description                    |
| -------------------------------- | ------------------------------ |
| <code>Texture &#124; null</code> | * The Texture that was removed |
</article>


---------------------------------------

<article>

### castToBaseTexture 
**Type:** <code>() -&#62; BaseTexture</code>

**Description:** Utility function for BaseTexture&#124;Texture cast.

#### Returns:
| Type                     | Description |
| ------------------------ | ----------- |
| <code>BaseTexture</code> |             |
</article>


---------------------------------------

<article>

### clone 
**Type:** <code>() -&#62; Texture</code>

**Description:** Creates a new texture object that acts the same as this one.

#### Returns:
| Type                 | Description       |
| -------------------- | ----------------- |
| <code>Texture</code> | * The new texture |
</article>


---------------------------------------

<article>

### destroy 
**Type:** <code>(destroyBase&#58; boolean) -&#62; void</code>

**Description:** Destroys this texture

#### Parameters:
| Name        | Type                 | Description                                 |
| ----------- | -------------------- | ------------------------------------------- |
| destroyBase | <code>boolean</code> | Whether to destroy the base texture as well |
</article>


---------------------------------------

<article>

### update 
**Type:** <code>() -&#62; void</code>

**Description:** Updates this texture on the gpu.
Calls the TextureResource update.

If you adjusted `frame` manually, please call `updateUvs()` instead.

</article>


---------------------------------------

<article>

### updateUvs 
**Type:** <code>() -&#62; void</code>

**Description:** Updates the internal WebGL UV cache. Use it after you change `frame` or `trim` of the texture. Call it after changing the frame

</article>


---------------------------------------

<article>

### onBaseTextureUpdated 
**Type:** <code>(baseTexture&#58; BaseTexture) -&#62; void</code>

**Description:** Called when the base texture is updated

#### Parameters:
| Name        | Type                     | Description       |
| ----------- | ------------------------ | ----------------- |
| baseTexture | <code>BaseTexture</code> | The base texture. |
</article>


---------------------------------------


    