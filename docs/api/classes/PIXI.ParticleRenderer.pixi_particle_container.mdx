---
id: "PIXI.ParticleRenderer.pixi_particle_container"
title: "Class: PIXI.ParticleRenderer"
sidebar_label: "PIXI.ParticleRenderer"
custom_edit_url: null
---


<div class="package-name">

[@pixi/particle-container](../modules/pixi_particle_container).ParticleRenderer

</div>


Renderer for Particles that is designer for speed over feature set.



        

<article>

### Constructor:
<h4><code>new PIXI.ParticleRenderer(renderer&#58; Renderer) </code></h4>

#### Parameters:
| Name     | Type                  | Default       | Description                               |
| -------- | --------------------- | ------------- | ----------------------------------------- |
| renderer | <code>Renderer</code> | <code></code> | The renderer this sprite batch works for. |

</article>

---------------------------------------


## Extends
- [PIXI.ObjectRenderer](../classes/PIXI.ObjectRenderer.pixi_core)

## Members
<article>

### shader 
**Type:** <code>Shader</code>

**Description:** The default shader that is used if a sprite doesn't have a more specific one.

</article>


---------------------------------------

<article>

### state 
**Type:** <code>State</code>

**Description:** The WebGL state in which this renderer will work.

</article>


---------------------------------------

<article>

### renderer 
**Type:** <code>Renderer</code>

**Description:** The renderer this manager works for.

</article>


---------------------------------------


## Methods
<article>

### destroy 
**Type:** <code>() -&#62; void</code>

**Description:** Destroys the ParticleRenderer.

</article>


---------------------------------------

<article>

### flush 
**Type:** <code>() -&#62; void</code>

**Description:** Stub method that should be used to empty the current batch by rendering objects now.

</article>


---------------------------------------

<article>

### render 
**Type:** <code>(container&#58; ParticleContainer) -&#62; void</code>

**Description:** Renders the particle container object.

#### Parameters:
| Name      | Type                           | Description                                          |
| --------- | ------------------------------ | ---------------------------------------------------- |
| container | <code>ParticleContainer</code> | The container to render using this ParticleRenderer. |
</article>


---------------------------------------

<article>

### start 
**Type:** <code>() -&#62; void</code>

**Description:** Stub method that initializes any state required before rendering starts. It is different from the `prerender`signal, which occurs every frame, in that it is called whenever an object requests _this_ renderer specifically.

</article>


---------------------------------------

<article>

### stop 
**Type:** <code>() -&#62; void</code>

**Description:** Stops the renderer. It should free up any state and become dormant.

</article>


---------------------------------------

<article>

### uploadPosition 
**Type:** <code>(children&#58; Sprite[], startIndex&#58; number, amount&#58; number, array&#58; number[], stride&#58; number, offset&#58; number) -&#62; void</code>

**Description:** Uploads the position.

#### Parameters:
| Name       | Type                  | Description                                                    |
| ---------- | --------------------- | -------------------------------------------------------------- |
| children   | <code>Sprite[]</code> | the array of sprites to render                                 |
| startIndex | <code>number</code>   | the index to start from in the children array                  |
| amount     | <code>number</code>   | the amount of children that will have their positions uploaded |
| array      | <code>number[]</code> | The vertices to upload.                                        |
| stride     | <code>number</code>   | Stride to use for iteration.                                   |
| offset     | <code>number</code>   | Offset to start at.                                            |
</article>


---------------------------------------

<article>

### uploadRotation 
**Type:** <code>(children&#58; Sprite[], startIndex&#58; number, amount&#58; number, array&#58; number[], stride&#58; number, offset&#58; number) -&#62; void</code>

**Description:** Uploads the rotation.

#### Parameters:
| Name       | Type                  | Description                                                   |
| ---------- | --------------------- | ------------------------------------------------------------- |
| children   | <code>Sprite[]</code> | the array of sprites to render                                |
| startIndex | <code>number</code>   | the index to start from in the children array                 |
| amount     | <code>number</code>   | the amount of children that will have their rotation uploaded |
| array      | <code>number[]</code> | The vertices to upload.                                       |
| stride     | <code>number</code>   | Stride to use for iteration.                                  |
| offset     | <code>number</code>   | Offset to start at.                                           |
</article>


---------------------------------------

<article>

### uploadTint 
**Type:** <code>(children&#58; Sprite[], startIndex&#58; number, amount&#58; number, array&#58; number[], stride&#58; number, offset&#58; number) -&#62; void</code>

**Description:** Uploads the tint.

#### Parameters:
| Name       | Type                  | Description                                                   |
| ---------- | --------------------- | ------------------------------------------------------------- |
| children   | <code>Sprite[]</code> | the array of sprites to render                                |
| startIndex | <code>number</code>   | the index to start from in the children array                 |
| amount     | <code>number</code>   | the amount of children that will have their rotation uploaded |
| array      | <code>number[]</code> | The vertices to upload.                                       |
| stride     | <code>number</code>   | Stride to use for iteration.                                  |
| offset     | <code>number</code>   | Offset to start at.                                           |
</article>


---------------------------------------

<article>

### uploadUvs 
**Type:** <code>(children&#58; Sprite[], startIndex&#58; number, amount&#58; number, array&#58; number[], stride&#58; number, offset&#58; number) -&#62; void</code>

**Description:** Uploads the UVs.

#### Parameters:
| Name       | Type                  | Description                                                   |
| ---------- | --------------------- | ------------------------------------------------------------- |
| children   | <code>Sprite[]</code> | the array of sprites to render                                |
| startIndex | <code>number</code>   | the index to start from in the children array                 |
| amount     | <code>number</code>   | the amount of children that will have their rotation uploaded |
| array      | <code>number[]</code> | The vertices to upload.                                       |
| stride     | <code>number</code>   | Stride to use for iteration.                                  |
| offset     | <code>number</code>   | Offset to start at.                                           |
</article>


---------------------------------------

<article>

### uploadVertices 
**Type:** <code>(children&#58; Sprite[], startIndex&#58; number, amount&#58; number, array&#58; number[], stride&#58; number, offset&#58; number) -&#62; void</code>

**Description:** Uploads the vertices.

#### Parameters:
| Name       | Type                  | Description                                                   |
| ---------- | --------------------- | ------------------------------------------------------------- |
| children   | <code>Sprite[]</code> | the array of sprites to render                                |
| startIndex | <code>number</code>   | the index to start from in the children array                 |
| amount     | <code>number</code>   | the amount of children that will have their vertices uploaded |
| array      | <code>number[]</code> | The vertices to upload.                                       |
| stride     | <code>number</code>   | Stride to use for iteration.                                  |
| offset     | <code>number</code>   | Offset to start at.                                           |
</article>


---------------------------------------


    