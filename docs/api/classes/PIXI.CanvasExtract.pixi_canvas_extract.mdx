---
id: "PIXI.CanvasExtract.pixi_canvas_extract"
title: "Class: PIXI.CanvasExtract"
sidebar_label: "PIXI.CanvasExtract"
custom_edit_url: null
---


<div class="package-name">

[@pixi/canvas-extract](../modules/pixi_canvas_extract).CanvasExtract

</div>

The extract manager provides functionality to export content from the renderers.
An instance of this class is automatically created by default, and can be found at `renderer.extract`



        

<article>

### Constructor:
<h4><code>new PIXI.CanvasExtract(renderer&#58; CanvasRenderer) </code></h4>

#### Parameters:
| Name     | Type                        | Default       | Description                         |
| -------- | --------------------------- | ------------- | ----------------------------------- |
| renderer | <code>CanvasRenderer</code> | <code></code> | A reference to the current renderer |

</article>

---------------------------------------




## Members
<article>

### renderer 
**Type:** <code>CanvasRenderer</code>

**Description:** A reference to the current renderer

</article>


---------------------------------------


## Methods
<article>

### base64 
**Type:** <code>(target&#58; DisplayObject &#124; RenderTexture, format&#58; string, quality&#58; number) -&#62; Promise&#60;string&#62;</code>

**Description:** Will return a base64 encoded string of this target. It works by calling`CanvasExtract.getCanvas` and then running toDataURL on that.

#### Parameters:
| Name    | Type                                            | Description                                                                           |
| ------- | ----------------------------------------------- | ------------------------------------------------------------------------------------- |
| target  | <code>DisplayObject &#124; RenderTexture</code> | A displayObject or renderTexture to convert. If left empty will use the main renderer |
| format  | <code>string</code>                             | Image format, e.g. "image/jpeg" or "image/webp".                                      |
| quality | <code>number</code>                             | JPEG or Webp compression from 0 to 1\. Default is 0.92.                               |
#### Returns:
| Type                                 | Description                             |
| ------------------------------------ | --------------------------------------- |
| <code>Promise&#60;string&#62;</code> | A base64 encoded string of the texture. |
</article>


---------------------------------------

<article>

### canvas 
**Type:** <code>(target&#58; DisplayObject &#124; RenderTexture, frame&#58; Rectangle) -&#62; ICanvas</code>

**Description:** Creates a Canvas element, renders this target to it and then returns it.

#### Parameters:
| Name   | Type                                            | Description                                                                           |
| ------ | ----------------------------------------------- | ------------------------------------------------------------------------------------- |
| target | <code>DisplayObject &#124; RenderTexture</code> | A displayObject or renderTexture to convert. If left empty will use the main renderer |
| frame  | <code>Rectangle</code>                          | The frame the extraction is restricted to.                                            |
#### Returns:
| Type                 | Description                                    |
| -------------------- | ---------------------------------------------- |
| <code>ICanvas</code> | A Canvas element with the texture rendered on. |
</article>


---------------------------------------

<article>

### destroy 
**Type:** <code>() -&#62; void</code>

**Description:** Destroys the extract

</article>


---------------------------------------

<article>

### image 
**Type:** <code>(target&#58; DisplayObject &#124; RenderTexture, format&#58; string, quality&#58; number) -&#62; Promise&#60;HTMLImageElement&#62;</code>

**Description:** Will return a HTML Image of the target

#### Parameters:
| Name    | Type                                            | Description                                                                           |
| ------- | ----------------------------------------------- | ------------------------------------------------------------------------------------- |
| target  | <code>DisplayObject &#124; RenderTexture</code> | A displayObject or renderTexture to convert. If left empty will use the main renderer |
| format  | <code>string</code>                             | Image format, e.g. "image/jpeg" or "image/webp".                                      |
| quality | <code>number</code>                             | JPEG or Webp compression from 0 to 1\. Default is 0.92.                               |
#### Returns:
| Type                                           | Description              |
| ---------------------------------------------- | ------------------------ |
| <code>Promise&#60;HTMLImageElement&#62;</code> | HTML Image of the target |
</article>


---------------------------------------

<article>

### pixels 
**Type:** <code>(target&#58; DisplayObject &#124; RenderTexture, frame&#58; Rectangle) -&#62; Uint8ClampedArray</code>

**Description:** Will return a one-dimensional array containing the pixel data of the entire texture in RGBA order, with integer values between 0 and 255 (included).

#### Parameters:
| Name   | Type                                            | Description                                                                           |
| ------ | ----------------------------------------------- | ------------------------------------------------------------------------------------- |
| target | <code>DisplayObject &#124; RenderTexture</code> | A displayObject or renderTexture to convert. If left empty will use the main renderer |
| frame  | <code>Rectangle</code>                          | The frame the extraction is restricted to.                                            |
#### Returns:
| Type                           | Description                                                           |
| ------------------------------ | --------------------------------------------------------------------- |
| <code>Uint8ClampedArray</code> | One-dimensional array containing the pixel data of the entire texture |
</article>


---------------------------------------


    